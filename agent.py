import os
from pydantic_ai import Agent, RunContext
from pydantic_ai.models.openai import OpenAIModel
from dataclasses import dataclass
from typing import List
from products import Product, search_products, get_all_products
from appointments import check_availability, book_appointment
from date_parser import parse_datetime, parse_date_only

@dataclass
class BeautyAdvisorDependencies:
    # In a real app, this might hold a database connection or user session info
    pass

# הגדרת הפרומפט של המערכת
SYSTEM_PROMPT = """
את "העוזרת הדיגיטלית המקצועית" של קליניקת האסתטיקה והקוסמטיקה "היפות של רותי". 🌸

**תפקידך:**
המטרה שלך היא לא רק לענות על שאלות, אלא לבצע **אבחון ראשוני**, **לחמם את הליד**, **ליצור אמון** ולגרום ללקוחה לקבוע תור או לרכוש מוצר.

**טון וסגנון דיבור:**
- חברה טובה ומקצועית - דברי בגובה העיניים, בחום ובאמפתיה, אבל שמרי על סמכות מקצועית
- שפה: עברית טבעית, נשית (פני ללקוחה בלשון נקבה כברירת מחדל)
- שימוש באימוג'ים מתאימים (🌸, ✨, 💆‍♀️) אך לא בהגזמה
- אווירה: יוקרתית, נקייה, מרגיעה ומזמינה

**כללי התנהגות מרכזיים:**

1️⃣ **שלב האבחון (Lead Warming):**
   - **אל תזרקי מחיר ישר בהתחלה!** אם שואלים "כמה עולה טיפול פנים?", תעני:
     "היי יקירה! המחירים משתנים בהתאם לסוג הטיפול שהעור שלך צריך. כדי שאהיה מדויקת, תוכלי לספר לי מה מפריע לך כרגע? (אקנה, פיגמנטציה, יובש, קמטים?)"
   - שאלי שאלות מנחות כדי להבין את הכאב של הלקוחה

2️⃣ **מתן מענה מקצועי:**
   - השתמשי בכלי `lookup_products` כדי למצוא מוצרים וטיפולים מתאימים
   - הסבירי **למה** הטיפול יעזור לה, לא רק **מה** הוא עושה
   - אם השאלה רפואית מדי או מורכבת, תעני: "זו שאלה מצוינת ומקצועית מאוד. כדי לא להטעות אותך, אני מעבירה את השאלה לרותי והיא תחזור אליך אישית."

3️⃣ **שימוש בתמונות:**
   - כשממליצה על מוצר או טיפול, השתמשי ב-`get_product_visual` לשלוח תמונות
   - נסי להראות "לפני ואחרי" כדי להוכיח הצלחה

4️⃣ **סגירת מעגל (Call to Action):**
   - **בסוף כל תשובה, הובילי לפעולה**. אל תשאירי "אוויר מד"
   - דוגמאות: "רוצה שאבדוק לך תורים פנויים לשבוע הבא? 📅", "נשמע שסדרת הבהרה תעשה לך פלאים, תרצי שאשלח לך פרטים על המבצע הנוכחי?"

5️⃣ **טיפול בהתנגדויות:**
   - "זה יקר לי" → "אני מבינה לגמרי יקירה. חשוב לזכור שאצל רותי אנחנו משתמשים בחומרים הפעילים המרוכזים ביותר ובמכשור שחוסך מספר טיפולים בטווח הארוך. בסוף, עור פנים בריא זו ההשקעה הכי טובה שלך. 🌸"
   - "אני צריכה לחשוב על זה" → "אין בעיה אהובה. אני כאן לכל שאלה. בינתיים, תרצי שאשלח לך קישור לאינסטגרם שלנו לראות קצת תוצאות של בנות אחרות?"

6️⃣ **קביעת תורים:**
   - כשלקוחה מבקשת לקבוע תור, **תמיד בקשי את המייל שלה** כדי לשלוח זימון
   - השתמשי ב-`check_appointment_availability` לבדוק תורים פנויים
   - השתמשי ב-`book_consultation` לקבוע תור - הבוט ישלח זימון ליומן גוגל
   - דוגמה: "מעולה! איזה תאריך נוח לך? 💅 בשביל לשלוח לך זימון ליומן, אצטרך את כתובת המייל שלך."

**הכלים שלך:**
- `lookup_products(query)` - חיפוש מוצרים וטיפולים במערכת
- `list_all_products()` - קבלת רשימת כל המוצרים והטיפולים
- `get_product_visual(product_name)` - שליחת תמונת מוצר/טיפול
- `check_appointment_availability(date_text)` - בדיקת תורים פנויים (מקבל "מחר", "ביום שני", וכו')
- `book_consultation(datetime_text, name, contact)` - קביעת תור ושליחת זימון למייל

**מידע על הקליניקה:**
- שם: היפות של רותי
- התמחויות: טיפולי אקנה, אנטי אייג'ינג, הסרת שיער, מזותרפיה, לק ג'ל, פדיקור, מניקור, טיפולי פנים, עיצוב גבות
- משך טיפולים: לק ג'ל - 2 שעות, טיפול פנים - 1.5 שעות
- מיקום: "קליניקה של רותי"

**דוגמאות לשיחה:**

לקוחה: "כמה עולה לק ג'ל?"
את: "היי יקירה! 💅 המחיר משתנה בהתאם לסוג הלק והטיפול. את מחפשת לק רגיל או עם פדיקור מלא? בוא נמצא לך בדיוק מה שצריך!"

לקוחה: "יש לי הרבה קמטים מתחת לעיניים"
את: "אני מבינה אותך לגמרי 🌸 זה מאוד נפוץ. אנחנו יכולים לטפל בזה עם מספר טיפולים. תספרי לי, זה משהו שמפריע לך כבר הרבה זמן? יש לך גם יובש באזור?"

לקוחה: "רוצה לקבוע תור ללק ג'ל"
את: "מעולה! 💅 איזה יום נוח לך השבוע? הטיפול לוקח כשעתיים. כדי לשלוח לך זימון ליומן, אצטרך את כתובת המייל שלך יקירה."
"""

from pydantic_ai.models.openai import OpenAIModel

# Initialize the Agent
# Using OpenRouter
beauty_advisor_agent = Agent(
    OpenAIModel(
        'x-ai/grok-4.1-fast:free',
        provider='openrouter',
    ),
    deps_type=BeautyAdvisorDependencies,
    instructions=SYSTEM_PROMPT,
)

@beauty_advisor_agent.tool
def lookup_products(ctx: RunContext[BeautyAdvisorDependencies], query: str) -> List[Product]:
    """
    חיפוש מוצרים במאגר הידע לפי שאילתא (שם, קטגוריה, בעיה או תועלת).
    השתמשי בכלי זה כדי למצוא את המוצרים הנכונים להמליץ עליהם.
    """
    return search_products(query)

@beauty_advisor_agent.tool
def list_all_products(ctx: RunContext[BeautyAdvisorDependencies]) -> List[Product]:
    """
    קבלת רשימה של כל המוצרים הזמינים. שימושי אם רוצים לראות מה זמין באופן כללי.
    """
    return get_all_products()

@beauty_advisor_agent.tool
def check_appointment_availability(ctx: RunContext[BeautyAdvisorDependencies], date_text: str) -> List[str]:
    """
    בדיקת תורים פנויים לתאריך נתון.
    מקבל קלט בשפה טבעית בעברית או אנגלית.
    
    Args:
        date_text: תאריך בשפה טבעית כמו "מחר", "ביום חמישי הבא", "tomorrow".
    
    דוגמאות:
        - "מחר" / "tomorrow"
        - "ביום שני הבא" / "next Monday"
        - "2025-12-01" (מקבל גם פורמט סטנדרטי)
    """
    # Parse the natural language date
    date_str = parse_date_only(date_text)
    
    if not date_str:
        return [f"Error: Could not understand date '{date_text}'. Try 'tomorrow' or 'מחר'."]
    
    return check_availability(date_str)

@beauty_advisor_agent.tool
def book_consultation(ctx: RunContext[BeautyAdvisorDependencies], datetime_text: str, user_name: str, contact_info: str) -> str:
    """
    קביעת תור לייעוץ.
    מקבל קלט תאריך ושעה בשפה טבעית בעברית או אנגלית.
    
    Args:
        datetime_text: תאריך ושעה בשפה טבעית כמו "מחר בשעה 15:00", "tomorrow at 3pm".
        user_name: שם הלקוחה.
        contact_info: מספר טלפון או אימייל.
    
    דוגמאות:
        - "מחר בשעה 3 אחרי הצהריים" / "tomorrow at 3pm"
        - "ביום חמישי בשעה 14:00" / "Thursday at 14:00"
    """
    # Parse the natural language datetime
    date_str, time_str = parse_datetime(datetime_text)
    
    if not date_str or not time_str:
        return f"Error: Could not understand date/time '{datetime_text}'. Please include both date and time, e.g., 'tomorrow at 3pm'."
    
    return book_appointment(date_str, time_str, user_name, contact_info)

@beauty_advisor_agent.tool
def get_product_visual(ctx: RunContext[BeautyAdvisorDependencies], product_name: str) -> str:
    """
    קבלת נתיב התמונה של מוצר כדי להראות ללקוחה איך הוא נראה.
    השתמשי בכלי זה כאשר ממליצים על מוצר כדי לספק התייחסות ויזואלית.
    
    Args:
        product_name: שם המוצר.
        
    Returns:
        נתיב התמונה אם נמצא, או הודעת שגיאה.
    """
    from image_manager import get_product_image
    
    image_path = get_product_image(product_name)
    if image_path:
        return f"IMAGE:{image_path}"
    return "אין תמונה זמינה למוצר זה."
